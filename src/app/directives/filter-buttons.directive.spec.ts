import { FilterButtonsDirective } from './filter-buttons.directive';

describe('FilterButtonsDirective', () => {
  it('should create an instance', () => {
    const directive = new FilterButtonsDirective();
    expect(directive).toBeTruthy();
  });
});
